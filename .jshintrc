{
    "boss": false,     // true: Tolerate assignments where comparisons would be expected
    "camelcase": true, // true: Require camelcase variable names.
    "curly": true,    // true: Require {} for every new block or scope
    "eqeqeq": true,   // true: Require triple equals (===) for comparison
    "eqnull": true,   // true: Tolerate use of `== null`
    "immed": true,    // true: Require immediate invocations to be wrapped in parens e.g. `(function () { } ());`
    "latedef": "nofunc",  // true: Require variables/functions to be defined before being used
    "maxlen": 130,    // max length of a line.
    "newcap": true,   // true: Require capitalization of all constructor functions e.g. `new F()`
    "noarg": true,    // true: Prohibit use of `arguments.caller` and `arguments.callee`
    "node": true,     // Node.js
    "nonbsp": true,   // true: This option warns about "non-breaking whitespace" characters.
    "sub": true,      // true: Tolerate using `[]` notation when it can still be expressed in dot notation
    "undef": true,    // true: Require all non-global variables to be declared (prevents global leaks)
    "unused": true,   // true: warns when you define and never use your variables.
    "-W117": true     // true: Ignore `not defined` errors as an example of using a rule (W117) by code.
}
